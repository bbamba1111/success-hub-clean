/**
 * Configuration for the /1day offer — "Make Time For More on Mondays™:
 * Design & Install Your First (or Next) Work-Life Balance Business Day™."
 *
 * This is a NEW $1,997 live, full-day, guided GROUP experience. It is
 * intentionally separate from the $497 SamCart offer embedded on /monday
 * (product id 1122707) — this offer must never reuse that checkout.
 *
 * ⚠️ TODO — REAL CHECKOUT URL REQUIRED BEFORE LAUNCH ⚠️
 * The preferred checkout/scheduling provider for this offer is Paperbell.
 * No Paperbell checkout or booking URL exists anywhere in this codebase yet
 * (confirmed via full-repo search), so a real one has NOT been invented
 * here. Replace the empty string below with the live Paperbell checkout URL
 * for the $1,997 "Make Time For More on Mondays™" package once it exists.
 *
 * Until this is set, the /1day CTA renders as a disabled "Coming Soon"
 * state instead of linking anywhere — it will NOT silently point at a fake
 * or unrelated URL.
 */
export const ONE_DAY_CHECKOUT_URL = ""

export const ONE_DAY_PRICE_LABEL = "$1,997"

export const ONE_DAY_OFFER_NAME = "Make Time For More on Mondays™"
